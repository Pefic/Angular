//file per fare i controlli
import { MiadDirective } from './miad.directive';

describe('MiadDirective', () => {
  it('should create an instance', () => {
    const directive = new MiadDirective();
    expect(directive).toBeTruthy();
  });
});
